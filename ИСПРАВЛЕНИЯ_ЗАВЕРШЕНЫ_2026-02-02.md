# Исправления завершены - 2026-02-02 16:25

## ✅ Что исправлено

### 1. ✅ Отключено скачивание видео при первом скрапинге
**Проблема**: Скрапер скачивал все 40 видео при добавлении аккаунта
**Решение**: 
- Добавлен параметр `downloadVideo` в `getAccountReels()` (default: `false`)
- При первом скрапинге (`scrape-account-reels`): `downloadVideo = false`
- При ежедневном обновлении (`update-tracked-reels`): `downloadVideo = true`

**Файлы**:
- `lib/apify.ts` - добавлен параметр
- `app/api/laba/scrape-account-reels/route.ts` - передаем `false`
- `app/api/cron/update-tracked-reels/route.ts` - передаем `true`

### 2. ✅ Заменен смайлик на 🥳
**Проблема**: В уведомлении о новых видео был смайлик 👋
**Решение**: Заменен на 🥳 ТОЛЬКО в сообщении "новые видео аккаунта"

**Файл**: `lib/telegram.ts` строка 231
- Было: `*👋 новые видео аккаунта @${account.instagram_username}*`
- Стало: `*🥳 новые видео аккаунта @${account.instagram_username}*`

**НЕ изменено**: Приветственное сообщение "аккаунт добавлен" - там остался 👋

---

## ⚠️ Что нужно проверить

### 1. Плашка "новое" не появляется
**Возможные причины**:
1. SQL миграция не выполнена в Supabase
2. Cron не запускался или не работает
3. Данные не обновляются

**Проверка SQL**:
```sql
-- Проверить что колонки существуют
SELECT column_name, data_type 
FROM information_schema.columns 
WHERE table_name = 'laba_reels' 
AND column_name IN ('is_new', 'marked_new_at');
```

Если колонок нет - выполнить:
```sql
ALTER TABLE laba_reels 
ADD COLUMN IF NOT EXISTS is_new BOOLEAN DEFAULT false;

ALTER TABLE laba_reels 
ADD COLUMN IF NOT EXISTS marked_new_at TIMESTAMP;

CREATE INDEX IF NOT EXISTS idx_laba_reels_is_new 
ON laba_reels(is_new) WHERE is_new = true;

CREATE INDEX IF NOT EXISTS idx_laba_reels_marked_new_at 
ON laba_reels(marked_new_at) WHERE is_new = true;
```

**Проверка данных**:
```sql
-- Посмотреть есть ли reels с is_new = true
SELECT id, instagram_reel_id, is_new, marked_new_at, created_at
FROM laba_reels
WHERE tracked_account_id IS NOT NULL
ORDER BY created_at DESC
LIMIT 10;
```

**Тест cron**:
```bash
curl https://service-production-f0b1.up.railway.app/api/cron/update-tracked-reels
```

### 2. Уведомления не приходят
**Возможные причины**:
1. Cron не настроен на cron-job.org
2. Настройки уведомлений отключены в БД
3. Telegram bot token неверный

**Проверка настроек**:
```sql
-- Проверить настройки уведомлений
SELECT * FROM laba_notification_settings;
```

**Проверка cron-job.org**:
- URL: https://cron-job.org
- Endpoint: `https://service-production-f0b1.up.railway.app/api/cron/update-tracked-reels`
- Schedule: `0 5 * * *` (каждый день в 5:00 UTC = 8:00 МСК)

---

## 📊 Коммит

**Backend**: `371650c`
**Репозиторий**: metaflora-app/service
**Ветка**: main
**Статус**: ✅ Запушено в GitHub

**Изменения**:
- `lib/apify.ts` (+3 строки)
- `app/api/laba/scrape-account-reels/route.ts` (+1 строка)
- `app/api/cron/update-tracked-reels/route.ts` (+1 строка)
- `lib/telegram.ts` (+1 строка)

---

## 🚀 Деплой

Railway автоматически задеплоит изменения из main ветки.

**Проверить деплой**:
1. Открыть https://railway.app
2. Проект: metaflora-service
3. Deployments → проверить что новый деплой запустился
4. Logs → проверить что нет ошибок

---

## 🧪 Тестирование

### Тест 1: Добавление аккаунта (не должно скачивать видео)
1. Открыть мини-апп → Лаба → Поиск аккаунта
2. Добавить новый аккаунт в отслеживаемые
3. Проверить логи Railway → НЕ должно быть "Downloading video"
4. Проверить что reels появились в БД

### Тест 2: Ежедневное обновление (должно скачивать видео)
1. Запустить cron вручную: `curl https://service-production-f0b1.up.railway.app/api/cron/update-tracked-reels`
2. Проверить логи Railway → ДОЛЖНО быть "Downloading video"
3. Проверить что пришло уведомление в Telegram с смайликом 🥳

### Тест 3: Плашка "новое"
1. Запустить cron
2. Проверить в БД: `SELECT * FROM laba_reels WHERE is_new = true`
3. Открыть мини-апп → Лаба → Отслеживаемые
4. Проверить что на новых reels есть плашка "новое"

---

## 📝 Следующие шаги

1. ✅ Проверить SQL миграцию в Supabase
2. ✅ Настроить cron на cron-job.org (если еще не настроен)
3. ✅ Дождаться деплоя на Railway
4. ✅ Протестировать добавление аккаунта
5. ✅ Запустить cron вручную для проверки уведомлений

---

**Дата**: 2026-02-02 16:25
**Backend commit**: `371650c`
**Статус**: ✅ Исправления применены и запушены
