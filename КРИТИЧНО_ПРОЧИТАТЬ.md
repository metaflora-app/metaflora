# 🔥 КРИТИЧНО: ПРОБЛЕМА С SUPABASE RLS

**Дата:** 2026-01-30  
**Статус:** Апify работает, но данные НЕ сохраняются в Supabase

---

## ❌ ПРОБЛЕМА

Apify возвращает 80 результатов, но они **не попадают в Supabase**.

**Это может быть только:**
1. ✅ **RLS (Row Level Security) блокирует INSERT** ← САМАЯ ВЕРОЯТНАЯ ПРИЧИНА
2. Переменные окружения неправильные (исправлено в коммите `1446544`)

---

## ✅ РЕШЕНИЕ: ОТКЛЮЧИТЬ RLS

### Шаг 1: Зайти в Supabase SQL Editor

Откройте: https://supabase.com/dashboard/project/lwjsbflvsmscfrdkejia/sql/new

### Шаг 2: Выполнить SQL

Скопируйте и выполните:

```sql
-- ОТКЛЮЧИТЬ RLS ДЛЯ ВСЕХ ТАБЛИЦ ЛАБЫ
ALTER TABLE laba_tracked_accounts DISABLE ROW LEVEL SECURITY;
ALTER TABLE laba_reels DISABLE ROW LEVEL SECURITY;
ALTER TABLE laba_analysis DISABLE ROW LEVEL SECURITY;
ALTER TABLE laba_scenarios DISABLE ROW LEVEL SECURITY;
ALTER TABLE laba_favorites DISABLE ROW LEVEL SECURITY;
ALTER TABLE laba_top_reels DISABLE ROW LEVEL SECURITY;
ALTER TABLE laba_notification_settings DISABLE ROW LEVEL SECURITY;
```

### Шаг 3: Проверить что RLS отключен

```sql
-- Должно показать FALSE для всех таблиц
SELECT 
  tablename,
  rowsecurity AS rls_enabled
FROM pg_tables 
WHERE schemaname = 'public' 
  AND tablename LIKE 'laba_%'
ORDER BY tablename;
```

### Шаг 4: Подождать деплоя Railway (2-3 минуты)

Коммит `1446544` сейчас деплоится.

### Шаг 5: Попробовать поиск снова

В мини-аппе попробуйте поиск - должен заработать!

---

## 🔍 ПРОВЕРКА

### Проверить что API работает:
```bash
curl -X POST "https://service-production-f0b1.up.railway.app/api/laba/search-reels" \
  -H "Content-Type: application/json" \
  -d '{"keyword":"нейросети","userId":994500304}'
```

Должно вернуть `{"success":true,"reels":[...]}`

### Проверить что данные в Supabase:
https://supabase.com/dashboard/project/lwjsbflvsmscfrdkejia/editor

Таблица `laba_reels` должна заполниться!

---

## 📋 ЧТО УЖЕ ИСПРАВЛЕНО

1. ✅ **API URL** в мини-аппе (коммит `34b44bc`)
2. ✅ **Правильные Apify Actors** (коммит `839728a`)
3. ✅ **Mapping полей** для разных форматов (коммит `a35940b`)
4. ✅ **Переменные окружения** - поддержка VITE_ (коммит `1446544`)
5. ⏳ **RLS** - НУЖНО ОТКЛЮЧИТЬ ВРУЧНУЮ В SUPABASE!

---

## ⚠️ КРИТИЧНО

**БЕЗ ОТКЛЮЧЕНИЯ RLS НИЧЕГО НЕ ЗАРАБОТАЕТ!**

RLS блокирует все INSERT запросы от `anon` ключа.

**Выполните SQL выше прямо сейчас!**

---

**После отключения RLS и деплоя все заработает!** 🚀
