# 📋 ПОДРОБНЕЙШАЯ ИНСТРУКЦИЯ: Воссоздание экранов через Figma Dev Links

## 🎯 Цель процесса
Создать экран с **ТОЧНЫМ** позиционированием через поэлементную сборку из Figma dev links.

---

## 🔄 ПОЛНЫЙ ПРОЦЕСС (10 шагов)

### ШАГ 1: Создание TODO листа
**Что делает AI:**
Инициализирует чек-лист для отслеживания прогресса.

---

### ШАГ 2: Получить dev ссылку на весь экран
**Что делает AI:**
1. `user-Figma-get_screenshot` - получает скриншот всего фрейма.
2. `user-Figma-get_design_context` - получает структуру слоев.
3. Описывает основные блоки и их расположение.

---

### ШАГ 3: Поэлементная сборка (КРИТИЧНО)
**Правила "Идеального Переноса":**

1.  **PNG Assets (ЗОЛОТОЕ ПРАВИЛО):**
    - Если элемент имеет сложные градиенты, тени или нестандартную форму — **ВСЕГДА** ищи готовую PNG на Desktop.
    - Никогда не пытайся имитировать сложные кнопки через CSS, если есть файл `кнопка ... .png`.
    - Всегда проверяй размеры PNG (`sips -g pixelWidth`) и ставь их `width/height` в коде 1-в-1.

2.  **Точные координаты (Insets):**
    - Используй `inset` или `left/top/width/height` строго из Figma metadata.
    - Если экран имеет фиксированную ширину (например, 1180px), используй `scale` трансформацию для адаптива.

3.  **Шрифты и Текст:**
    - Строго соблюдай `fontFamily` (Gotham Pro, Inter, TT Commons).
    - Для длинных текстов используй `whiteSpace: 'pre-wrap'` и проверяй переносы строк по Figma.
    - Если текст не влезает — уточни у юзера, нужно ли сократить или изменить контейнер.

4.  **Слои (DOM Order):**
    - Фоны (большие лого, паттерны) должны быть **ПЕРВЫМИ** в DOM дереве.
    - Карточки с `backdropFilter` должны идти **ПОСЛЕ** фонов, чтобы размытие работало.
    - Интерактивные элементы (всплывашки, бэджи) должны быть в самом конце с высоким `zIndex`.

---

### ШАГ 4: Скриншот-контроль
**Перед деплоем AI ОБЯЗАН:**
1. Сделать скриншот реализованного экрана.
2. Сделать скриншот фрейма из Figma.
3. Сравнить их визуально.
4. Если есть отличия (лого торчит, текст уехал) — **ИСПРАВИТЬ ДО ОТПРАВКИ**.

---

### ШАГ 5: Билд и Деплой
1. `npm run build` — проверка на ошибки TS.
2. `git add -A && git commit -m "feat: add screen-name (X/31)"`
3. `git push origin HEAD:main`

---

## 📊 Ожидания и отмашки

- **ТЫ:** Даешь ссылки на элементы.
- **AI:** Фиксирует каждый элемент, показывает ASCII схему и CSS.
- **ТЫ:** Даешь отмашку "теперь все".
- **AI:** Собирает компонент, делает финальную проверку.
- **ТЫ:** "деплой".

---

## 🎯 Результат
Экран в деплое должен быть **НЕОТЛИЧИМ** от фрейма в Figma.

---

**Версия процесса:** v5 (Идеальный перенос + PNG-приоритет)  
**Прогресс:** 16/31 экранов ✅
