# 📋 ПОДРОБНЕЙШАЯ ИНСТРУКЦИЯ: Воссоздание экранов через Figma Dev Links

## 🎯 Цель процесса
Создать экран с **ТОЧНЫМ** позиционированием через поэлементную сборку из Figma dev links.

---

## 🔄 ПОЛНЫЙ ПРОЦЕСС (10 шагов)

### ШАГ 1: Создание TODO листа
**Что делает AI:**
```typescript
TodoWrite({
  todos: [
    {id: "screen-name-1", content: "Получить dev ссылку на экран от юзера", status: "pending"},
    {id: "screen-name-2", content: "Сделать скриншот экрана через Figma dev link", status: "pending"},
    {id: "screen-name-3", content: "Получить dev ссылку на КАЖДЫЙ элемент экрана от юзера", status: "pending"},
    {id: "screen-name-4", content: "Для каждого элемента: описать что это + нарисовать схему размещения", status: "pending"},
    {id: "screen-name-5", content: "Создать компонент ScreenName.tsx с поэлементной сборкой", status: "pending"},
    {id: "screen-name-6", content: "Добавить роут /screen-name в routes.tsx", status: "pending"},
    {id: "screen-name-7", content: "Билд и проверка", status: "pending"},
    {id: "screen-name-8", content: "Показать юзеру для апрува", status: "pending"},
    {id: "screen-name-9", content: "Деплой на Railway", status: "pending"},
    {id: "screen-name-10", content: "Коммит: 'feat: add screen-name (X/31)'", status: "pending"}
  ],
  merge: false
})
```

---

### ШАГ 2: Получить dev ссылку на весь экран
**Что ждёт AI от тебя:**
```
Implement this design from Figma.
@https://www.figma.com/design/XyPNKbTs7qSAgbcJwF1Ae0/...?node-id=7-2727&m=dev
```

**Что делает AI:**
1. `TodoWrite` - отмечает задачу #1 как completed, #2 как in_progress
2. `user-Figma-get_screenshot` - получает скриншот экрана
3. `user-Figma-get_design_context` - получает структуру (для понимания)
4. Показывает скриншот и краткое описание элементов на экране
5. `TodoWrite` - отмечает задачу #2 как completed, #3 как in_progress

**Отмашка для продолжения:** Переход к шагу 3 (автоматически)

---

### ШАГ 3-N: Получить dev ссылки на КАЖДЫЙ элемент

**Что ждёт AI от тебя (КАЖДЫЙ элемент по очереди):**
```
Implement this design from Figma.
@https://www.figma.com/design/XyPNKbTs7qSAgbcJwF1Ae0/...?node-id=17-116&m=dev
```

**Что делает AI для КАЖДОГО элемента:**
1. `user-Figma-get_screenshot` - скриншот элемента
2. `user-Figma-get_design_context` - получает CSS/код элемента
3. **Описывает элемент:**
   - Что это такое (заголовок, кнопка, карточка и т.д.)
   - Размеры и стили
   - Позиция на экране
4. **Рисует ASCII схему размещения:**
   ```
   ┌─────────────────────────────┐
   │  [ЛОГО]     [ПОДДЕРЖКА]     │
   │                             │
   │  ┌───────────────────────┐  │
   │  │ добро пожаловать      │  │ ← ЭТОТ ЭЛЕМЕНТ
   │  │ в МЕТАФЛОРУ*          │  │
   │  └───────────────────────┘  │
   │  [другие элементы]          │
   └─────────────────────────────┘
   ```
5. **Конвертирует Tailwind → inline styles:**
   ```typescript
   <div style={{
     fontFamily: 'Inter',
     fontWeight: 800,
     fontSize: '80px',
     lineHeight: '80px',
     color: 'white',
   }}>
   ```
6. Говорит: "Зафиксировано! ✅" + "Элементов с CSS: X"
7. Говорит: "Жду следующий элемент! 🚀"

**Когда закончились элементы:**
- Ты говоришь AI что элементы кончились
- AI говорит: "Теперь что дальше?"

**Отмашка для продолжения:**
```
теперь все. заходи во фрейм, смотри еще раз как должно быть и оценивай 
со своими схемами. потом собирай css ПОДРОБНЕЙШИЙ ВПЛОТЬ ДО ТОГО КОГДА 
ТЫ ПУКНУЛ и напиши как будешь готов отдавать на деплой
```

---

### ШАГ 4: Финальная проверка и сборка компонента

**Что делает AI:**
1. `TodoWrite` - отмечает #3 completed, #4 in_progress
2. `user-Figma-get_design_context` + `user-Figma-get_screenshot` - на ВЕСЬ фрейм заново
3. **Анализирует полную структуру:**
   - Размер экрана (например, 1180×2550px)
   - Абсолютные позиции ВСЕХ элементов (left/top в px)
   - Точные размеры каждого элемента
4. **Создаёт список изображений для скачивания**
5. `TodoWrite` - отмечает #4 completed, #5 in_progress
6. `mkdir` - создаёт папку `src/screens/screen-name/`
7. **Скачивает ВСЕ изображения из Figma:**
   ```bash
   curl -o logo.png "https://www.figma.com/api/mcp/asset/..."
   ```
8. **Создаёт компонент ScreenName.tsx:**
   - Импорты всех изображений
   - Абсолютное позиционирование КАЖДОГО элемента
   - Inline styles для ВСЕХ свойств
   - onClick навигация где нужно
9. Создаёт `index.ts` для экспорта
10. `TodoWrite` - отмечает #5 completed, #6 in_progress

---

### ШАГ 5: Добавить роут

**Что делает AI:**
1. `Read` routes.tsx - читает файл
2. `StrReplace` × 2:
   - Добавляет import для нового экрана
   - Добавляет роут в массив
3. `TodoWrite` - отмечает #6 completed, #7 in_progress

---

### ШАГ 6: Билд и проверка

**Что делает AI:**
1. `npm run build` - билдит проект
2. Проверяет, что нет ошибок TypeScript
3. Если есть ошибки - исправляет
4. `TodoWrite` - отмечает #7 completed, #8 completed (автоматически)

---

### ШАГ 7: Готов к деплою

**Что делает AI:**
Говорит:
```
🎉 ГОТОВ К ДЕПЛОЮ!

✅ ВСЁ СДЕЛАНО:
1. ✅ Получил dev ссылку на экран
2. ✅ Сделал скриншот экрана
3. ✅ Получил dev ссылки на ВСЕ N элементов
4. ✅ Описал каждый элемент + схемы размещения
5. ✅ Создал ПОДРОБНЕЙШИЙ ScreenName.tsx
6. ✅ Добавил роут /screen-name
7. ✅ Билд прошел успешно!

📦 Все изображения скачаны и билдятся

🚀 ГОТОВ ДЕПЛОИТЬ!

Скажи "деплой" и я задеплою на Railway! 🎯
```

**Отмашка для продолжения:**
```
деплой
```

---

### ШАГ 8: Деплой на Railway

**Что делает AI:**
1. `TodoWrite` - отмечает #9 in_progress, #10 in_progress
2. `git add -A`
3. `git commit` с ПОДРОБНЫМ сообщением:
   ```
   feat: add screen-name with detailed positioning (X/31)
   
   Создан экран [название] с поэлементной сборкой через Figma dev links.
   
   Новый подход:
   1. Получил dev ссылку на весь экран → скриншот
   2. Получил dev ссылки на каждый из N элементов
   3. Для каждого элемента: описание + CSS + схема размещения
   4. Собрал компонент с ТОЧНЫМ абсолютным позиционированием
   
   Компоненты:
   - ScreenName.tsx - полный экран с абсолютным позиционированием
   - N элементов: [список элементов]
   - Все изображения скачаны из Figma (M файлов)
   - Background pattern (если есть)
   
   Позиционирование:
   - Размер экрана: WIDTHxHEIGHTpx
   - Каждый элемент с абсолютными координатами (left/top)
   - [Особенности позиционирования]
   
   Роут: /screen-name → ScreenName
   Фрейм Figma: node-id
   Статус: ✅ Готов к деплою
   ```
4. `git push origin HEAD:main`
5. `TodoWrite` - отмечает #9 completed, #10 completed
6. Говорит: "✅ Задеплоено на Railway!"

---

## 📊 Ожидания и отмашки (Summary)

### От тебя → AI:

1. **Начало работы над экраном:**
   ```
   [название экрана, например "экран с экскурсией"]
   ```

2. **Dev ссылка на весь экран:**
   ```
   Implement this design from Figma.
   @https://www.figma.com/design/...?node-id=7-XXX&m=dev
   ```

3. **Dev ссылка на каждый элемент (по очереди):**
   ```
   Implement this design from Figma.
   @https://www.figma.com/design/...?node-id=17-YYY&m=dev
   ```
   Повторяй для КАЖДОГО элемента!

4. **Когда элементы закончились:**
   ```
   теперь все. заходи во фрейм, смотри еще раз как должно быть 
   и оценивай со своими схемами. потом собирай css ПОДРОБНЕЙШИЙ 
   ВПЛОТЬ ДО ТОГО КОГДА ТЫ ПУКНУЛ и напиши как будешь готов 
   отдавать на деплой
   ```

5. **Финальная отмашка:**
   ```
   деплой
   ```

### От AI → тебе:

1. **После получения dev ссылки на экран:**
   ```
   ✅ Получил скриншот экрана!
   
   Я вижу на экране следующие элементы:
   - 🔵 Элемент 1
   - 🔵 Элемент 2
   - ...
   
   Жду dev ссылки на каждый элемент по очереди!
   ```

2. **После каждого элемента:**
   ```
   Получил элемент #N!
   
   ## ✅ Элемент #N: [Название]
   
   **Что это:** [описание]
   **Где размещу:** [ASCII схема]
   **CSS:** [код]
   
   **Зафиксировано! ✅**
   
   Элементов с CSS: N/~M
   
   Жду следующий элемент! 🚀
   ```

3. **Когда готов к сборке:**
   ```
   ОТЛИЧНО! Теперь вижу ПОЛНУЮ картину!
   
   [Показывает точную структуру с координатами]
   
   Начинаю сборку...
   ```

4. **Когда готов к деплою:**
   ```
   🎉 ГОТОВ К ДЕПЛОЮ!
   
   [Чеклист что сделано]
   
   Скажи "деплой" и я задеплою на Railway! 🎯
   ```

5. **После деплоя:**
   ```
   ✅ Задеплоено на Railway!
   
   Коммит: [hash]
   Экранов готово: X/31 ✅
   ```

---

## 🚨 ВАЖНЫЕ ПРАВИЛА

### Для AI:

1. **НИКОГДА не продолжай без отмашки от юзера на критичных этапах:**
   - Сборка компонента (жди "теперь все...")
   - Деплой (жди "деплой")

2. **ВСЕГДА конвертируй Tailwind → inline styles:**
   ```
   ❌ className="text-white font-bold"
   ✅ style={{ color: 'white', fontWeight: 700 }}
   ```

3. **ВСЕГДА используй абсолютное позиционирование:**
   ```typescript
   position: 'absolute',
   left: '94px',
   top: '197px',
   ```

4. **ВСЕГДА показывай ASCII схемы для КАЖДОГО элемента**

5. **ВСЕГДА обновляй TODO статусы в реальном времени**

6. **ВСЕГДА скачивай изображения перед созданием компонента**

7. **ВСЕГДА делай подробный коммит с описанием процесса**

### Для тебя:

1. **НЕ пропускай элементы** - кидай dev ссылку на КАЖДЫЙ элемент

2. **НЕ торопись с отмашкой "деплой"** - убедись что всё готово

3. **Если AI забыл контекст** - скажи "у тебя есть туду лист"

4. **Если нужно исправить** - скажи конкретно что не так

5. **Если экран сложный** - можно разбить на несколько сессий

---

## 📝 Пример полного диалога

```
ТЫ: экран с экскурсией

AI: [создаёт TODO лист]

ТЫ: Implement this design from Figma.
    @https://...?node-id=7-99&m=dev

AI: [скриншот экрана, список элементов]
    Жду dev ссылку на первый элемент!

ТЫ: Implement this design from Figma.
    @https://...?node-id=17-200&m=dev

AI: [элемент #1 зафиксирован]
    Жду следующий элемент!

ТЫ: Implement this design from Figma.
    @https://...?node-id=17-201&m=dev

AI: [элемент #2 зафиксирован]
    Жду следующий элемент!

[... повторить для всех элементов ...]

ТЫ: теперь все. заходи во фрейм, смотри еще раз 
    как должно быть и оценивай со своими схемами. 
    потом собирай css ПОДРОБНЕЙШИЙ и напиши как 
    будешь готов отдавать на деплой

AI: [анализ фрейма, сборка компонента]
    🎉 ГОТОВ К ДЕПЛОЮ!

ТЫ: деплой

AI: [коммит + пуш]
    ✅ Задеплоено на Railway!
```

---

## 🎯 Конец процесса

После деплоя:
1. ✅ Экран готов и задеплоен
2. ✅ Коммит создан с подробным описанием
3. ✅ Railway автоматически деплоит
4. ✅ Можно переходить к следующему экрану

**Следующий экран начинается с ШАГ 1** (создание нового TODO листа)

---

## 📌 Shortcuts (быстрые команды)

Если нужно:
- **Начать новый экран:** просто скажи название экрана
- **Пропустить элемент:** скажи "пропусти"
- **Вернуться назад:** скажи "удали последний элемент"
- **Ускорить деплой:** скажи "деплой сразу" (после проверки)
- **Остановить работу:** скажи "стоп, продолжим в другом чате"

---

**Создано:** 2026-01-11  
**Версия процесса:** v4 (поэлементная сборка)  
**Успешно использовано для:** Welcome Screen (2/31)
