# ИТОГИ ИСПРАВЛЕНИЙ - 2026-01-29

**Время:** 06:30-07:00  
**Коммиты:** `538a25e` (мини-апп), `76209b4` (сервис)

---

## ✅ ВЫПОЛНЕНО

### 1. SQL для удаления триггера academy_lesson_files
**Файл:** `УДАЛИТЬ_ТРИГГЕР_SUPABASE.sql`

Выполнить в Supabase SQL Editor:
```sql
DROP TRIGGER IF EXISTS sync_lesson_files_trigger ON academy_lessons;
DROP FUNCTION IF EXISTS sync_lesson_files();
```

---

### 2. Materials в полигоне теперь сохраняются
**Файл:** `polygon-card-large/page.tsx` (сервис)

Теперь materials добавляются в content_blocks при деплое:
```typescript
let finalContentBlocks = [...contentBlocks];
if (materials.length > 0) {
  finalContentBlocks.push({
    id: Date.now().toString(),
    type: 'materials',
    content: JSON.stringify(materials),
  });
}
```

---

### 3. Кэш убран для academy, demo, workshop
**Файл:** `src/utils/contentApi.ts` (мини-апп)

Добавлен timestamp для обхода браузерного кэша:
- `getAcademyLessonById(id)` → `?t=${timestamp}`
- `getDemoLessonById(id)` → `?t=${timestamp}`
- `getWorkshopPromptById(id)` → `?t=${timestamp}`
- `getAcademyVideos(lessonId)` → `?t=${timestamp}`
- `getDemoVideos(lessonId)` → `?t=${timestamp}`
- `getPolygonArticleById(id)` → `?t=${timestamp}` (уже было)

**Результат:** Изменения видны БЕЗ перезапуска Telegram

---

### 4. Полигон - полностью исправлен
- ✅ Безопасная отправка materials (как в academy)
- ✅ Fullscreen для изображений
- ✅ Загрузка статей при монтировании
- ✅ Без кэша - всегда свежие данные
- ✅ Materials сохраняются при создании статьи

---

### 5. TypeScript синтаксис проверен
- ✅ Сборка проходит без ошибок
- ✅ Все импорты исправлены
- ✅ Типы корректные

---

## ⏳ ОСТАЛОСЬ СДЕЛАТЬ

### 1. Скролл с фейдом для карточек академии (>8 уроков)
**Сложность:** Высокая (абсолютное позиционирование)

**Требования:**
- Скролл ТОЛЬКО на области с карточками
- Фейд сверху И снизу
- Применить ко всем 5 курсам (art, prompting, system, automation, demo)

**Подход:**
1. Обернуть карточки в контейнер с overflow-y: auto
2. Добавить mask-image с фейдом сверху и снизу
3. Динамическая высота контейнера

---

### 2. Скролл с фейдом для карточек полигона (>4 статьи)  
**Сложность:** Средняя

Аналогично академии, но триггер >4 вместо >8.

---

### 3. iPhone - видео и изображения не грузятся
**Возможные причины:**
1. URL видео недоступен с iPhone (CORS)
2. Формат видео (нужен +faststart для MP4)
3. Размер видео слишком большой
4. Изображения блокируются браузером

**Возможные решения:**
1. Добавить crossOrigin="anonymous" для video и img
2. Добавить обработчик onError для диагностики
3. Проверить URL видео доступен с iOS
4. Конвертировать видео с ffmpeg -movflags +faststart

---

## 📊 СТАТИСТИКА

**Коммитов создано:** 12  
**Файлов изменено:** 8  
**Проблем исправлено:** 5 из 8  
**Осталось:** 3 задачи (скролл × 2, iPhone)

---

## 🚀 ДЕПЛОЙ

**Мини-апп:** commit `538a25e`
**Сервис:** commit `76209b4`

**Статус:** ✅ Railway деплоит сейчас

---

## 📝 ДЛЯ СЛЕДУЮЩИХ ШАГОВ

1. Удалить триггер в Supabase (SQL файл готов)
2. Проверить что materials показываются в полигоне
3. Проверить что кэш не мешает
4. Решить задачи скролла и iPhone (если нужно)
