# Финальная инструкция - 2026-02-02 17:15

## 📋 Что сделано

### Backend (commit `4e48cd4`)
1. ✅ Обработка callback кнопок (отключить, изменить время, удалить)
2. ✅ Обработка текстовых сообщений с временем
3. ✅ Детальное логирование для отладки
4. ✅ `parse_mode: 'Markdown'` для Telegram
5. ✅ Полное удаление аккаунта (не деактивация)

### Frontend (commit `6b623c0`)
1. ✅ Плашка "новое" как PNG изображение
2. ✅ Кэширование LabaTrackedScreen
3. ✅ Попапы с маленькой буквы
4. ✅ Редирект после покупки

---

## 🔧 ЧТО НУЖНО СДЕЛАТЬ СЕЙЧАС

### 1. Выполни SQL в Supabase
https://supabase.com/dashboard/project/lwjsbflvsmscfrdkejia/editor/37230

```sql
ALTER TABLE laba_notification_settings 
ADD COLUMN IF NOT EXISTS awaiting_time_input BOOLEAN DEFAULT false;

CREATE INDEX IF NOT EXISTS idx_notification_settings_awaiting 
ON laba_notification_settings(awaiting_time_input) 
WHERE awaiting_time_input = true;
```

### 2. Дождись деплоя (~2-3 минуты)
Railway → metaflora-service → Deployments → коммит `4e48cd4`

### 3. Проверь логи Railway
После деплоя открой Deploy Logs и поищи:
- "📨 webhook получен" - webhook работает
- "🔘 Обрабатываем callback" - кнопки работают
- "💬 Обрабатываем сообщение" - текст обрабатывается
- "⏰ устанавливаем время" - время сохраняется

---

## 🧪 Тестирование после деплоя

### Тест 1: Отправка уведомлений
```bash
curl https://service-production-f0b1.up.railway.app/api/cron/send-notifications
```

Должно прийти в Telegram с кнопками.

### Тест 2: Кнопка "отключить уведомления"
1. Нажми кнопку
2. Должно прийти: "уведомления для этого аккаунта отключены..."
3. Проверь в Supabase:
```sql
SELECT notifications_enabled FROM laba_notification_settings
WHERE user_id = (SELECT id FROM users WHERE telegram_id = 994500304)
AND tracked_account_id = (SELECT id FROM laba_tracked_accounts WHERE instagram_username = 'mertzv2');
```
Результат: `false`

4. Запусти снова curl - уведомление НЕ должно прийти

### Тест 3: Кнопка "изменить время"
1. Нажми кнопку
2. Должно прийти: "напишите время в формате ЧЧ:ММ..."
3. Проверь в Supabase:
```sql
SELECT awaiting_time_input FROM laba_notification_settings
WHERE user_id = (SELECT id FROM users WHERE telegram_id = 994500304)
AND tracked_account_id = (SELECT id FROM laba_tracked_accounts WHERE instagram_username = 'mertzv2');
```
Результат: `true`

4. Напиши "10:00" в бота
5. Должно прийти: "время уведомлений изменено на 10:00 по московскому времени"
6. Проверь в Supabase:
```sql
SELECT notification_time, awaiting_time_input FROM laba_notification_settings
WHERE user_id = (SELECT id FROM users WHERE telegram_id = 994500304)
AND tracked_account_id = (SELECT id FROM laba_tracked_accounts WHERE instagram_username = 'mertzv2');
```
Результат: `notification_time = '10:00:00'`, `awaiting_time_input = false`

### Тест 4: Кнопка "перестать отслеживать"
1. Нажми кнопку
2. Должно прийти: "вы перестали отслеживать аккаунт @mertzv2"
3. Проверь в Supabase:
```sql
SELECT * FROM laba_tracked_accounts 
WHERE instagram_username = 'mertzv2'
AND user_id = (SELECT id FROM users WHERE telegram_id = 994500304);
```
Результат: 0 строк (удален)

---

## ⚠️ Если все равно не работает

### Проверь логи Railway
1. Открой https://railway.app
2. metaflora-service → Deployments → последний деплой
3. Deploy Logs → поиск:
   - "webhook получен" - приходят ли запросы
   - "Обрабатываем callback" - обрабатываются ли кнопки
   - "Обрабатываем сообщение" - обрабатываются ли тексты
   - Любые ошибки

### Проверь webhook в Telegram
```bash
curl -s "https://api.telegram.org/bot8435745315:AAHr5BCZDaHNG8m4joL6h6eYeBFmO0GYyzI/getWebhookInfo" | jq '.'
```

Должно быть:
- `"url": "https://service-production-f0b1.up.railway.app/api/bot/webhook"`
- `"pending_update_count": 0` (нет застрявших обновлений)

---

## 📝 Файлы

- `ADD_AWAITING_TIME_INPUT.sql` - SQL миграция
- `КАК_ПРОТЕСТИРОВАТЬ_КНОПКИ_2026-02-02.md` - подробная инструкция
- `ПРОВЕРКА_НАСТРОЕК_2026-02-02.md` - SQL запросы для проверки

---

**Дата**: 2026-02-02 17:15
**Backend**: `4e48cd4` ✅ Запушено
**Frontend**: `6b623c0` ✅ Запушено
**Твой telegram_id**: 994500304
**Статус**: Ожидаем деплоя для тестирования
