# Исправления - 2026-02-02 16:20

## 🔍 Проблемы

### 1. ❌ Плашка "новое" не появляется на новых reels
**Причина**: Логика работает, но возможно миграция SQL не выполнена или данные не обновляются

### 2. ❌ Уведомления не приходят о новых reels
**Причина**: Нужно проверить логи cron и настройки уведомлений

### 3. ❌ Скрапер скачивает все 40 видео при первом отслеживании
**Причина**: `includeDownloadedVideo: true` в `lib/apify.ts` строка 155
**Решение**: Добавить параметр `downloadVideo` и передавать `false` при первом отслеживании

### 4. ❌ Неправильный смайлик в уведомлениях
**Текущий**: 👋
**Нужный**: 🥳
**Файлы**: 
- `lib/telegram.ts` строка 231
- `lib/telegram.ts` строка 286

---

## ✅ Исправления

### 1. Отключить скачивание видео при первом отслеживании

**Файл**: `/Users/user/Desktop/metaflora-service/lib/apify.ts`

**Изменить функцию `getAccountReels`**:
```typescript
export async function getAccountReels(
  username: string,
  resultsLimit: number = 20,
  onlyPostsNewerThan?: string, // YYYY-MM-DD format
  downloadVideo: boolean = false // НОВЫЙ ПАРАМЕТР
): Promise<InstagramReelData[]>
```

**Строка 155**: заменить на:
```typescript
includeDownloadedVideo: downloadVideo, // Скачивать видео только для уведомлений
```

---

### 2. Обновить вызовы в track-account

**Файл**: `/Users/user/Desktop/metaflora-service/app/api/laba/track-account/route.ts`

Найти вызов `getAccountReels` (должен быть около строки 150-200) и добавить `false`:
```typescript
const reelsData = await getAccountReels(username, 40, undefined, false);
```

---

### 3. Обновить вызовы в update-tracked-reels

**Файл**: `/Users/user/Desktop/metaflora-service/app/api/cron/update-tracked-reels/route.ts`

**Строка 90**: заменить на:
```typescript
const reelsData = await getAccountReels(username, 40, oneDayAgoFormatted, true);
```

---

### 4. Заменить смайлик на 🥳

**Файл**: `/Users/user/Desktop/metaflora-service/lib/telegram.ts`

**Строка 231**: заменить на:
```typescript
let messageText = `*🥳 новые видео аккаунта @${account.instagram_username}*\n\n`;
```

**Строка 286**: заменить на:
```typescript
const message = `🥳 *аккаунт (название аккаунта) добавлен в отслеживаемые*
```

---

## 📋 SQL миграция - проверка

Нужно проверить что в Supabase выполнены миграции:

### Проверка колонок
```sql
-- Проверить laba_reels
SELECT column_name, data_type 
FROM information_schema.columns 
WHERE table_name = 'laba_reels' 
AND column_name IN ('is_new', 'marked_new_at');

-- Должно вернуть:
-- is_new | boolean
-- marked_new_at | timestamp without time zone
```

Если колонок нет - выполнить:
```sql
ALTER TABLE laba_reels 
ADD COLUMN IF NOT EXISTS is_new BOOLEAN DEFAULT false;

ALTER TABLE laba_reels 
ADD COLUMN IF NOT EXISTS marked_new_at TIMESTAMP;

CREATE INDEX IF NOT EXISTS idx_laba_reels_is_new ON laba_reels(is_new) WHERE is_new = true;
CREATE INDEX IF NOT EXISTS idx_laba_reels_marked_new_at ON laba_reels(marked_new_at) WHERE is_new = true;
```

---

## 🎯 Порядок действий

1. ✅ Исправить `lib/apify.ts` - добавить параметр `downloadVideo`
2. ✅ Исправить `app/api/laba/track-account/route.ts` - передать `false`
3. ✅ Исправить `app/api/cron/update-tracked-reels/route.ts` - передать `true`
4. ✅ Исправить `lib/telegram.ts` - заменить 👋 на 🥳 (2 места)
5. ✅ Проверить SQL миграцию в Supabase
6. ✅ Закоммитить и задеплоить backend
7. ✅ Протестировать добавление аккаунта (не должно скачивать видео)
8. ✅ Запустить cron вручную для проверки уведомлений

---

**Дата**: 2026-02-02 16:20
**Backend**: /Users/user/Desktop/metaflora-service
**Статус**: Готово к исправлению
