# Финальные исправления - 2026-02-02 16:35

## ✅ Backend исправления

### 1. Убрана плашка "новое" при первом скрапинге
**Проблема**: Все 40 reels получали `is_new = true` при добавлении аккаунта
**Решение**: 
- `lib/apify.ts`: `is_new = false` по умолчанию
- Плашка "новое" ставится ТОЛЬКО в cron при добавлении новых reels

**Коммит**: `631a96d`

### 2. SQL для сброса флагов
Создан файл `RESET_IS_NEW_FLAGS.sql`:
```sql
UPDATE laba_reels
SET is_new = false, marked_new_at = NULL
WHERE is_new = true;
```

**Выполнить в Supabase** чтобы сбросить все текущие флаги.

---

## ✅ Frontend исправления

### 1. Кэширование LabaTrackedScreen
**Проблема**: При возврате появлялись пустые плашки и хардкод-данные
**Решение**:
- Кэширование `accounts` в sessionStorage
- Кэширование `reels` в sessionStorage  
- Кэширование `selectedAccountId` в sessionStorage
- Данные загружаются из кэша при инициализации

**Коммит**: `0f364dd`

---

## 📊 Деплой

### Backend
**Коммит**: `631a96d` ✅ Запушено
**URL**: https://service-production-f0b1.up.railway.app

### Frontend
**Коммит**: `0f364dd` ✅ Запушено
**URL**: https://web-production-fc84.up.railway.app

Railway автоматически задеплоит оба репозитория (~2-3 минуты).

---

## 🎯 Что изменилось

### До:
1. При добавлении аккаунта → все 40 reels с плашкой "новое" ❌
2. При возврате на экран → пустые плашки и хардкод-данные ❌

### После:
1. При добавлении аккаунта → reels БЕЗ плашки "новое" ✅
2. Плашка "новое" появляется ТОЛЬКО после cron (новые reels за день) ✅
3. При возврате на экран → данные из кэша, без пустых плашек ✅

---

## 🔧 Что нужно сделать

### 1. Сбросить флаги в Supabase
Выполнить SQL из `RESET_IS_NEW_FLAGS.sql`:
```sql
UPDATE laba_reels
SET is_new = false, marked_new_at = NULL
WHERE is_new = true;
```

### 2. Дождаться деплоя
- Backend: ~2-3 минуты
- Frontend: ~2-3 минуты

### 3. Протестировать
1. Добавить новый аккаунт → reels БЕЗ плашки "новое"
2. Перейти назад и вернуться → данные не пропадают
3. Запустить cron → новые reels С плашкой "новое"

---

## 📝 Логика плашки "новое"

### Когда НЕ ставится:
- ❌ При первом скрапинге (добавление аккаунта)
- ❌ При ручном обновлении reels

### Когда ставится:
- ✅ В cron `/api/cron/update-tracked-reels`
- ✅ Только на новые reels за последние 24 часа
- ✅ Автоматически убирается через 24 часа

---

**Дата**: 2026-02-02 16:35
**Backend**: `631a96d` ✅
**Frontend**: `0f364dd` ✅
**Статус**: Готово к деплою
