# Исправление кнопок callback - 2026-02-02 17:05

## ❌ Проблемы

1. Кнопка "перестать отслеживать аккаунт" - деактивировала вместо удаления
2. Кнопка "изменить время" - не сохраняла время
3. Кнопка "отключить уведомления" - работала, но сообщение неинформативное

## ✅ Исправления

### 1. handleUntrack - полное удаление
**Было**: `update({ is_active: false })`
**Стало**: `delete()`

```typescript
// ПОЛНОЕ УДАЛЕНИЕ из БД (не деактивация)
const { error } = await supabase
  .from('laba_tracked_accounts')
  .delete()
  .eq('id', accountId)
  .eq('user_id', user.id);
```

### 2. handleChangeTime - временное решение
**Было**: TODO комментарий
**Стало**: Сообщение что функция в разработке

```typescript
await sendTelegramMessage(
  userId,
  `функция изменения времени уведомлений будет доступна в следующем обновлении. сейчас уведомления приходят в 08:00 по московскому времени`
);
```

### 3. handleDisableNotifications - улучшенное сообщение
**Было**: `уведомления успешно отключены`
**Стало**: `уведомления для этого аккаунта отключены. вы можете включить их обратно в мини-приложении`

### 4. Добавлено логирование
- Логи для отладки в каждом обработчике
- Логи ошибок при удалении/обновлении

---

## 🚀 Деплой

**Коммит**: `63dfdaf` ✅ Запушено

Railway задеплоит изменения (~2-3 минуты).

---

## 🧪 Тестирование

После деплоя отправь снова уведомление:
```bash
curl https://service-production-f0b1.up.railway.app/api/cron/send-notifications
```

Нажми на кнопки:

1. **"отключить уведомления"**
   - Должно прийти: "уведомления для этого аккаунта отключены..."
   - В БД: `notifications_enabled = false`

2. **"изменить время"**
   - Должно прийти: "функция изменения времени будет доступна..."

3. **"перестать отслеживать аккаунт"**
   - Должно прийти: "вы перестали отслеживать аккаунт @mertzv2"
   - В БД: аккаунт УДАЛЕН (не деактивирован)

---

## 📝 Проверка в Supabase

### До нажатия "перестать отслеживать":
```sql
SELECT id, instagram_username, is_active 
FROM laba_tracked_accounts 
WHERE instagram_username = 'mertzv2';
```

### После нажатия:
```sql
-- Аккаунта не должно быть
SELECT id, instagram_username, is_active 
FROM laba_tracked_accounts 
WHERE instagram_username = 'mertzv2';
-- Результат: 0 строк
```

---

**Дата**: 2026-02-02 17:05
**Коммит**: `63dfdaf`
**Статус**: Готово к тестированию после деплоя
