# Для следующего агента - 2026-02-02 17:17 ФИНАЛ

## 🔥 ОБЯЗАТЕЛЬНО ПРОЧИТАТЬ

**Главные файлы**:
1. `ФИНАЛЬНАЯ_ИНСТРУКЦИЯ_2026-02-02.md` - что сделано и что проверить
2. `ADD_AWAITING_TIME_INPUT.sql` - SQL миграция (НУЖНО ВЫПОЛНИТЬ)
3. `КАК_ПРОТЕСТИРОВАТЬ_КНОПКИ_2026-02-02.md` - тестирование кнопок

---

## 📊 Текущее состояние

**Frontend**: `c8225ce` → https://web-production-fc84.up.railway.app
**Backend**: `4e48cd4` → https://service-production-f0b1.up.railway.app

**Telegram Bot**: @metaflora_bot
**Telegram ID пользователя**: 994500304

---

## ✅ Что сделано сегодня

### Backend (5 коммитов)
1. ✅ Отключено скачивание видео при первом скрапинге
2. ✅ `is_new = false` по умолчанию (плашка только через cron)
3. ✅ Добавлен `parse_mode: 'Markdown'` в Telegram
4. ✅ Полное удаление аккаунта при "перестать отслеживать"
5. ✅ Обработка текстовых сообщений для изменения времени
6. ✅ Детальное логирование webhook

### Frontend (3 коммита)
1. ✅ Плашка "новое" как PNG изображение (не div)
2. ✅ Кэширование LabaTrackedScreen (accounts, reels, selectedAccountId)
3. ✅ Попапы с маленькой буквы, без !
4. ✅ Редирект на premium dashboard после покупки

---

## ⚠️ ЧТО НУЖНО ПРОВЕРИТЬ ЗАВТРА

### 1. Cron отработал?
Завтра в 9:00 по Москве должен отработать cron на cron-job.org.

**Проверить**:
- Пришли ли уведомления в Telegram о новых reels?
- Есть ли плашки "новое" на новых reels в мини-апе?

### 2. Кнопки работают?
**После деплоя** (сегодня вечером или завтра):
- Отправь уведомление: `curl https://service-production-f0b1.up.railway.app/api/cron/send-notifications`
- Нажми кнопки и проверь что они обновляют БД
- Проверь логи Railway → Deploy Logs

### 3. SQL миграция выполнена?
**НУЖНО ВЫПОЛНИТЬ** в Supabase:
```sql
ALTER TABLE laba_notification_settings 
ADD COLUMN IF NOT EXISTS awaiting_time_input BOOLEAN DEFAULT false;
```

Файл: `ADD_AWAITING_TIME_INPUT.sql`

---

## 🔧 Логика работы

### Плашка "новое"
- ❌ НЕ ставится при первом скрапинге (добавление аккаунта)
- ✅ Ставится ТОЛЬКО в cron на новые reels за день
- ✅ Автоматически убирается через 24 часа

### Скачивание видео
- ❌ НЕ скачивает при первом скрапинге (40 reels)
- ✅ Скачивает ТОЛЬКО в cron для отправки в Telegram

### Уведомления
- ✅ Отправляются через `/api/cron/send-notifications`
- ✅ Проверяют `notifications_enabled` в БД
- ✅ Смайлик 👋 (не 🥳)
- ✅ 3 кнопки: отключить, изменить время, удалить

### Кнопки
1. **"отключить уведомления"** → `notifications_enabled = false`
2. **"изменить время"** → ждет ввод времени, сохраняет в `notification_time`
3. **"перестать отслеживать"** → УДАЛЯЕТ аккаунт из БД

---

## 📁 Ключевые файлы

### Backend
- `lib/apify.ts` - параметр `downloadVideo`, `is_new = false`
- `lib/telegram.ts` - обработчики кнопок, `handleTimeMessage`
- `app/api/bot/webhook/route.ts` - webhook с логированием
- `app/api/cron/send-notifications/route.ts` - отправка уведомлений
- `app/api/cron/update-tracked-reels/route.ts` - ежедневный парсинг

### Frontend
- `src/components/ReelCard.tsx` - плашка "новое" как PNG
- `src/screens/laba-tracked/LabaTrackedScreen.tsx` - кэширование
- `src/screens/metacoins/MetacoinsScreen.tsx` - попапы, редирект

### SQL
- `ADD_AWAITING_TIME_INPUT.sql` - НУЖНО ВЫПОЛНИТЬ
- `RESET_IS_NEW_FLAGS.sql` - сброс флагов (если нужно)

---

## 🐛 Известные проблемы

### 1. Кнопки могут не работать до деплоя
**Причина**: Новый код еще не задеплоился
**Решение**: Дождись деплоя, проверь логи

### 2. Время не сохраняется
**Причина**: Колонка `awaiting_time_input` не добавлена в БД
**Решение**: Выполни SQL из `ADD_AWAITING_TIME_INPUT.sql`

### 3. Уведомления приходят в 9:00 МСК
**Причина**: Cron на cron-job.org настроен на 6:00 UTC
**Решение**: Измени на cron-job.org или используй кнопку "изменить время"

---

## 💡 Советы для следующего агента

1. **Логи в Railway** - проверяй Deploy Logs для отладки webhook
2. **SQL миграции** - всегда проверяй что колонки существуют
3. **Telegram webhook** - уже настроен, не трогай
4. **Cron на cron-job.org** - настроен на 6:00 UTC = 9:00 МСК
5. **Деплой ВСЕГДА в main** - БЕЗ force push

---

## 🎯 Что проверить завтра

1. **В 9:00 МСК** - должен отработать cron
2. **Проверить Telegram** - пришли ли уведомления?
3. **Проверить мини-апп** - есть ли плашки "новое"?
4. **Протестировать кнопки** - работают ли после деплоя?

---

**Дата**: 2026-02-02 17:17
**Frontend**: `c8225ce`
**Backend**: `4e48cd4`
**Статус**: ✅ ВСЁ ГОТОВО, ждем завтрашнего cron

**Готово к проверке завтра**:
- Cron в 9:00 МСК
- Уведомления в Telegram
- Плашки "новое" в мини-апе
- Кнопки управления уведомлениями
